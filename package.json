{
    "name": "to-do-list",
    "version": "1.0.0",
    "scripts": {
        "start": "npm --prefix ./server start & npm --prefix ./client start",
        "build": "npm --prefix ./client run build"
    },
    "dependencies": {
        "concurrently": "^5.3.0"
    }
}
